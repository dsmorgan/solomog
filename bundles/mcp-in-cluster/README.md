# Simple in-cluster MCP service routing

Note that there is no path defined, so every URL path is actually served. 
We are testing /mcp, but anything not defined in path will route to this.

It's likely bad idea to use a catch all.
