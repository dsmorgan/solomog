apiVersion: v1
kind: Service
metadata:
  name: mcp-website-fetcher
  namespace: mcp
  labels:
    app: mcp-website-fetcher
spec:
  selector:
    app: mcp-website-fetcher
  ports:
  - port: 80
    targetPort: 8000
    appProtocol: agentgateway.dev/mcp
